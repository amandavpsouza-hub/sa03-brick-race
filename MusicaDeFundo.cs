namespace BrickRace
{
    public static class MusicaDeFundo
    {
        public static void Tocar(string caminho)
        {
            // Implementação opcional: apenas sinaliza o início da música.
            // Neste jogo de console, mantém o comportamento sem falhas.
        }

        public static void Parar()
        {
            // Implementação opcional: não há áudio nativo no console.
        }
    }
}
